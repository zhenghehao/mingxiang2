# 交接说明

完整接手规则见根目录 `AGENTS.md`，日常与平台行为见
`docs/七平台草稿系统交接.md`。

当前版本将工作流成品整理成 `draft-manifest.json`，由内嵌 5409 草稿服务按平台
串行处理。主应用只接受已有 `runId` 与平台选择，不接受界面传入任意素材路径。

旧 8199 服务、正式发布提示和成功后删除封面的逻辑均已移除。五个视频平台只能
保存草稿；两个音频平台只能停在人工确认前。

